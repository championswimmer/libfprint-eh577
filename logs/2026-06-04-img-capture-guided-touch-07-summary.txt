# EH577 img-capture guided touch 07 summary

Date: 2026-06-04
Command used:

```bash
./tools/eh577_guided_img_capture.sh \
  --log "$PWD/logs/2026-06-04-img-capture-guided-touch-07.txt" \
  --label "EH577 img-capture guided touch 07 strong pacing" \
  --hold 16 \
  --timeout 35 \
  --frame-dump-dir "$PWD/dumps/2026-06-04-libfprint-guided-touch-07" \
  --pre-frame-delay-ms 100 \
  --poll-loop-delay-ms 50
```

## Result

The stronger pacing hooks were active and visible in the runtime log:

- repeated `Delaying frame request ... by 100 ms before sending 64 14 ec`
- repeated `Delaying next poll loop by 50 ms ...`

Observed output:

- `34` runtime frame dumps captured
- every frame remained:
  - `len=5356`
  - `nonzero=0`
- all `34` dumped frames had the same SHA-256 hash:
  - `bd2386946403b82cd740767b15507502a27a7d5b4c86440a5af0966c71ad8a38`

## Comparison

That hash is still exactly equal to the standalone idle post-init frame hash and still differs from both successful standalone finger-hold frame hashes.

So even strong pacing did **not** move the runtime off the idle-identical zero-frame path.

## Conclusion

At this point, simple timing/pacing changes are no longer the most likely missing ingredient.
The next evidence-backed step should be `usbmon` capture of a patched `img-capture` run so the runtime USB sequence can be compared directly against the successful standalone post-init finger-hold behavior.
