# EH577 img-capture guided touch 05 preflight failure summary

Date: 2026-06-04
Context: guided touch 05 was attempted with the new runtime frame-dump hook enabled.

## Result

The guided wrapper correctly aborted before starting `img-capture` because the live libusb preflight failed.

User-visible wrapper output showed:

- EH577 still present in sysfs and basic USB enumeration
- live libusb preflight failed
- guided run was not started

## Kernel evidence

During the same period, kernel log showed:

- `usb 3-3: USB disconnect, device number 4`
- repeated `device descriptor read/64, error -110`
- repeated re-enumeration attempts with new device numbers

## Meaning

This is a **USB-health blocker**, not a libfprint runtime result.
The new wrapper did its job by preventing another misleading guided-touch run on an unhealthy device.

## Next step

Recover EH577 to a healthy openable state again before retrying the frame-dump guided run.
Prefer the known non-cold recovery sequence if sudo is available; otherwise refresh sudo or cold reboot.
