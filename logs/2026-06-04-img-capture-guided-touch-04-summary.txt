# EH577 img-capture guided touch 04 summary

Date: 2026-06-04
Command used:

```bash
./tools/eh577_guided_img_capture.sh \
  --log "$PWD/logs/2026-06-04-img-capture-guided-touch-04.txt" \
  --label "EH577 img-capture guided touch 04" \
  --hold 12 \
  --timeout 25
```

## Result

This run successfully exercised the latest EH577 runtime logic.

Observed behavior:

- wrapper preflight passed
- device open succeeded
- post-init sequence completed successfully
- the driver stayed on **post-init polling** as intended
- repeated post-init `64 14 ec` responses were received successfully
- every observed large frame remained:
  - `len=5356`
  - `nonzero=0`
- finger status remained `off`
- no image was assembled or submitted
- no USB disconnect or open-time crash occurred during this run

## Important meaning

This removes the earlier uncertainty about the repeat-path behavior.
Even when EH577 is forced to stay on **post-init polling** under libfprint, the current runtime still sees only all-zero large frames during the guided touch run.

So the remaining blocker is no longer:

- PRE_INIT misbranching
- repeat-path-only polling
- transport failure
- or an open-time crash in this run

The next likely blocker is now one of:

1. the libfprint runtime still does not match the successful standalone finger-hold timing closely enough,
2. additional frame-state or command-state context is missing in the runtime loop,
3. or the current finger-detection / frame-handling path needs deeper inspection with raw frame dumps from inside libfprint.

## Recommended next step

Add targeted frame dumping or frame statistics inside the libfprint EH577 runtime path so the current libfprint-captured `64 14 ec` buffers can be compared directly against the known-good standalone post-init finger-hold dumps.
